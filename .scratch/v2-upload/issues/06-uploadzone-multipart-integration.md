# 06 — UploadZone 接入分片路径（浮动窗口也能上传大文件）

**What to build:** 现有的浮动上传窗口接入分片上传能力，让用户即使不打开独立页面也能上传 1GB 大文件、看到断点续传状态。完成后再上传大文件不再卡在 100MB 限制，断网也能自动续。

**Blocked by:** #05

**Status:** ready-for-agent

- [ ] `UploadZone` 的 `startUpload()` 改用新的 `uploadFiles()`（自动选直传/分片）
- [ ] 上传面板显示每个文件的当前阶段（OBS 上传 / 服务器处理）+ 网速
- [ ] 总进度按字节聚合（包括处理中文件已完成的字节）
- [ ] 大文件（>100MB）上传时面板顶部显示"分片上传中：part 3/10"
- [ ] 验收：上传 200MB ARW 通过浮动窗口完成，进度面板正确显示分片状态