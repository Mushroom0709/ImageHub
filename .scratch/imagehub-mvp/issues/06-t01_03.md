# 06 — 前后端联通 + CORS + 开发代理

**What to build:** 前端能调后端 /health 接口，开发环境 Vite 代理配置好，CORS 中间件配置。

**Blocked by:** T01-01, T01-02

**Status:** ready-for-agent

## Acceptance criteria

- [ ] 后端 CORS 中间件配置
- [ ] 前端 Vite dev server 代理 /api 到后端
- [ ] 前端调用 /api/health 成功拿到数据
- [ ] 开发体验流畅，热更新正常

## Notes

