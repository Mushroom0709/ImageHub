# 09 — 上传完成回调 API

**What to build:** POST /upload/complete 接口，登记上传完成的文件，触发后续处理（先打日志，后续 ticket 再加实际处理）。

**Blocked by:** T02-02

**Status:** ready-for-agent

## Acceptance criteria

- [ ] POST /api/upload/complete 接口
- [ ] 接收 uploadId + files 列表
- [ ] 写入 upload_log 表
- [ ] 返回 assetIds（先返回空，等 T04-01 再写 assets 表）
- [ ] Postman / curl 可测

## Notes

