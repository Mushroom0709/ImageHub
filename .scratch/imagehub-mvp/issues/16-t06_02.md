# 16 — 素材打标 + 取消标签 + 批量打标 API

**What to build:** 给素材打标签、取消标签、批量打标。

**Blocked by:** T04-01, T06-01

**Status:** ready-for-agent

## Acceptance criteria

- [ ] POST /api/assets/:id/tags 批量加标签
- [ ] DELETE /api/assets/:id/tags/:tagId 移除单个标签
- [ ] POST /api/assets/batch-tag 批量打标（支持加/删）
- [ ] asset_tags 表的增删正确
- [ ] 重复打标幂等（不会重复插入）
- [ ] 素材详情返回 tags 列表

## Notes

