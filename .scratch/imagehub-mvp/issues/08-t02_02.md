# 08 — 预签名 URL 生成 API

**What to build:** POST /upload/credentials 接口，批量生成预签名 URL。前端用 curl 验证直传成功。

**Blocked by:** T02-01

**Status:** ready-for-agent

## Acceptance criteria

- [ ] POST /api/upload/credentials 接口
- [ ] 支持批量（多个文件一次生成）
- [ ] 返回 uploadId + 每个文件的 uploadUrl + obsKey
- [ ] 前端用脚本直传测试通过
- [ ] 预签名 URL 过期时间合理（1小时）

## Notes

