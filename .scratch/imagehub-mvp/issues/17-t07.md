# 17 — 多标签交集筛选 API

**What to build:** 传入多个 tagId，返回同时拥有所有标签的素材列表，与分页排序兼容。

**Blocked by:** T06-02

**Status:** ready-for-agent

## Acceptance criteria

- [ ] GET /api/assets?tagIds=a,b,c 多标签筛选（AND 语义）
- [ ] SQL 级性能优化（asset_tags 联合索引）
- [ ] 与 sort/page/size/sourceType 等参数组合工作
- [ ] 1w 数据量级下响应 < 500ms（本地 PG 模拟）

## Notes

