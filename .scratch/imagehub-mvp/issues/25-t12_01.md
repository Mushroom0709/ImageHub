# 25 — EXIF 读取（JPG/PNG/WebP）

**What to build:** 上传后读取 EXIF 信息，写入 assets.exif JSON 字段，GET /assets/:id/exif 接口。

**Blocked by:** T05-01

**Status:** ready-for-agent

## Acceptance criteria

- [ ] exiftool 封装（或 Python exif 库）
- [ ] 读取：相机、镜头、光圈、快门、ISO、焦距、白平衡、拍摄时间、GPS
- [ ] 写入 assets.exif JSON 字段
- [ ] GET /api/assets/:id/exif 接口返回结构化 EXIF
- [ ] 支持常见格式：JPG/PNG/WebP/TIFF

## Notes

