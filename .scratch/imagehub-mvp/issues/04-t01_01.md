# 04 — 后端脚手架 + PG/Redis Docker Compose

**What to build:** FastAPI 后端项目结构跑起来，PG 和 Redis 通过 docker-compose 启动，/health 返回 ok，数据库连接成功。

**Blocked by:** T00-01（确认 OBS SDK 可用再搭）

**Status:** ready-for-agent

## Acceptance criteria

- [ ] FastAPI 项目结构：app/ (api/services/models/schemas/core) + main.py
- [ ] SQLAlchemy 2.0 + 基础配置
- [ ] docker-compose.yml：api + postgres + redis
- [ ] GET /health 返回 { status: ok, database: ok, redis: ok }
- [ ] Alembic 初始化，第一个 migration 可执行
- [ ] 本地 docker compose up 一键启动

## Notes

