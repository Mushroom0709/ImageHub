# CONTEXT — ImageHub

> 项目上下文文档。Agent 在做非平凡改动前必须读此文件。只含领域术语表，不含实现细节。

## 项目简介

ImageHub 是一套**纯标签驱动**的图片视频分类管理系统。素材通过多维度标签组织和检索，存储基于 OBS 对象存储。

## 术语表（Ubiquitous Language）

### 标签 (Tag)
- **全局统一**的检索维度，系统的核心组织方式
- 9 大分类：场景、风格、服装、妆容、姿势类型、构图、色调氛围、身材修饰点、信息（日期/相机/镜头/焦段）
- **支持无限层级**（父标签/子标签）和别名（同义词）
- 一个素材可以有多个标签
- 标签有状态：`active`（已确认）/ `pending`（AI 新建待确认）

### 素材 (Asset)
- 系统的最小单位 = **单张图片 或 单个视频**
- 每个素材独立打标、独立管理
- 同来源（如同一条小红书笔记）的多张图，通过**来源标签**关联，而非组实体
- 支持格式：JPG/PNG/WebP/GIF/TIFF 等常见图片 + **ARW（索尼 RAW）** + MP4/MOV/WEBM 视频

### 采集 (Collection Job)
- 从外部平台（小红书、抖音）批量获取素材的过程
- 同一条笔记采集出的多张图，各自独立为素材，通过来源标签（note_id / author / location 等）可追溯回原笔记
- **两层去重**：第一层 source_id + file_index（快速），第二层感知哈希 pHash（内容级，可选）

### AI 打标
- 采集/上传后 AI 自动打标签，直接生效
- 低置信度（< 阈值）的标签和 AI 新建的标签进入**待审核区**
- AI 可以**动态创建新标签**，新标签默认为 pending 状态

### 回收站 (Trash)
- 删除素材 = 软删除，进入回收站，保留 **15 天**
- 15 天后自动从 OBS 物理删除

### 星标 (Star)
- 最简标记：布尔值，快速收藏好图

### 旗标 (Flag)
- 分级标记：多级（1-5 或自定义颜色），用于标记优先级/处理状态

### 集合 (Collection)
- 手动精选的主题合集（如"西藏旅行参考"）
- 一个素材可属于多个集合
- MVP 阶段暂不实现，后续迭代

### EXIF
- 上传后自动读取 EXIF 信息（相机、镜头、光圈、快门、ISO、焦距、GPS、拍摄时间等）
- 详情页展示，RAW 文件同样支持

### 感知哈希 (pHash)
- 每张图计算 64 位感知哈希指纹
- 用于采集去重（第二层）和相似图查找
- 汉明距离判断相似度：<5 同图，<15 相似

### 缩略图
- 三档：小图（300px，列表用）/ 中图（1200px，详情预览用）/ 原图
- 全桶私有，通过预签名 URL 访问

## 约定

- 中文沟通，代码注释和提交信息用中文
- ticket 粒度：每个 1-3 小时可完成
- 改代码前先读相关文件，不许靠猜反复 patch
